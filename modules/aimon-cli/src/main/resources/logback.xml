<configuration>
    <!-- File only, on purpose: no log event reaches the terminal, which belongs to the REPL
         (docs/design/llm/provider-switch-agent-model-check.md relies on it). Do not define an appender here
         without the appender-ref that uses it: Logback reports an unreferenced appender as a WARN, and any
         WARN or ERROR while it configures itself makes it print its whole configuration status to stdout (#134).
    -->

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${user.home}/.aimon/logs/aimon.log</file>
        <encoder>
            <pattern>%d{ISO8601} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${user.home}/.aimon/logs/aimon-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- Aimon application logging -->
    <logger name="aimon" level="INFO"/>

    <!-- OpenAI client logging (reduce noise) -->
    <logger name="com.theokanning.openai" level="WARN"/>

    <!-- Root logger -->
    <root level="WARN">
        <appender-ref ref="FILE"/>
    </root>
</configuration>
